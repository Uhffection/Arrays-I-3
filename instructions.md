Starting with the `fruits` array provided, make three changes to the array. 

- Update the first item in the array to be "apricot" instead of "apple".
- Change "cherry" to "clementine".
- Add a seventh element, "grape", to the end of the array.

So after these three changes, the fruits array should read:

```
["apricot", "banana", "clementine", "dragon fruit", "elderberry", "fig", "grape"]
```

**Hint**: Arrays begin at an index of 0.

